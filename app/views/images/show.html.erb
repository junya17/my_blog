<div class="container">
<h2><%= @image.name %></h2>
<% if @image.image.attached? %>
   <%= image_tag @image.image ,class: "img-fluid p-3" %> 
<% end %>
<p>投稿日：<%= @image.created_at.strftime('%Y年%m月%d日 %H:%M') %></p>
</div>
